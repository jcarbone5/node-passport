<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header text-center">
                <h4 class="card-text">Profile</h4>
            </div>
            <div class="card-body text-center">
                <h5 class="card-text font-weight-bold">{{ user.username }}</h5>
                <h6 class="card-text">{{ user.name }} {{ user.lastname }}</h5>
                <h6 class="card-text">{{ user.email }}</h5>
            </div>
        </div>
    </div>
</div>